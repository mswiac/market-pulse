import { Component } from '@angular/core';

// Placeholder shell for Phase 3 (routing/guard/sidebar) so the lazy route
// resolves at build time — Phase 4 replaces this with the full form UI.
@Component({
  selector: 'app-admin-panel',
  templateUrl: './admin-panel.html',
  styleUrl: './admin-panel.scss',
})
export class AdminPanel {}
