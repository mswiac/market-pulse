# Compiled output
/dist
/tmp
/out-tsc
/bazel-out

# Node
/node_modules
npm-debug.log
yarn-error.log

# Angular
/.angular/cache
.sass-cache/
/connect.lock
/libpeerconnection.log
testem.log
/typings

# Cloudflare Workers / Wrangler
.wrangler/
.dev.vars

# Environment variables & secrets
.env
.env.local
.env.*.local

# Testing
/coverage
__screenshots__/
.stryker-tmp/
/reports

# IDEs and editors
.idea
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# Personal dotfiles (shell, editor, tools)
.bash_profile
.bashrc
.zshrc
.zprofile
.profile
.gitconfig
.ripgreprc
idea-notes.md

# MCP configuration (may contain tokens)
.mcp.json

# Claude Code / 10x-cli workspace — all contents are local/auto-managed,
# except the shared settings.json + hooks/ (PreToolUse guard blocking
# .dev.vars reads, see lessons.md "Never read or print a whole secrets file")
.claude/*
!.claude/settings.json
!.claude/hooks/
.agents/

# System / special files
.gitmodules

# System files
.DS_Store
Thumbs.db
